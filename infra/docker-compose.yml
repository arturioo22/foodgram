version: '3.8'

services:
  postgres:
    image: postgres:15-alpine
    environment:
      POSTGRES_DB: foodgram
      POSTGRES_USER: foodgram_user
      POSTGRES_PASSWORD: 'afdwMp-85pYexEuTJ27nLfLfxdw'
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U foodgram_user -d foodgram"]  # ← ИСПРАВЛЕНО
      interval: 10s
      timeout: 5s
      retries: 5
  
  backend:
    image: arturioo22/foodgram-backend:latest
    environment:
      DB_HOST: postgres
      DB_NAME: foodgram
      DB_USER: foodgram_user
      DB_PASSWORD: 'afdwMp-85pYexEuTJ27nLfLfxdw'
      SECRET_KEY: '-PpGq7VpK5vgk94e-7yTrL1soZMETDoUedrjnbvvPTkN6UrgAI6ViIIRsisFb1wfc5c'
      DEBUG: 'True'  # ← Временно включите DEBUG
      ALLOWED_HOSTS: 'fffoooddgramm.ddns.net,localhost,127.0.0.1'
    depends_on:
      postgres:
        condition: service_healthy
    volumes:
      - static_volume:/app/static/
      - media_volume:/app/media/
    command: >
      sh -c "python manage.py migrate --no-input &&
            python manage.py collectstatic --no-input &&
            gunicorn --bind 0.0.0.0:8000 foodgram.wsgi:application"
  
  nginx:
    image: nginx:alpine
    ports:
      - "80:80"
    volumes:
      - ./nginx/default.conf:/etc/nginx/conf.d/default.conf:ro
      - static_volume:/usr/share/nginx/html/static/
      - media_volume:/usr/share/nginx/html/media/
    depends_on:
      - backend

volumes:
  postgres_data:
  static_volume:
  media_volume: